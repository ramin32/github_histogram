Displays weekly commits from each repository for a given user using histograms via the protovis js library.

The interface is a simple web app using the Flask web framework.

Default username is ramin32 but may be modified at runtime.

TO RUN:
pip install -r requirements.txt
./app.py

Author:
Ramin Rakhamimov
ramin32@gmail.com
http://raminrakhamimov.tk
